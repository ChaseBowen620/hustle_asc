# Dashboard JWT login (set same values as frontend VITE_LOGIN_USERNAME / VITE_LOGIN_PASSWORD)
# Used by POST /api/auth/login/ to issue JWTs for register and other protected APIs
DASHBOARD_USERNAME=your-username@example.com
DASHBOARD_PASSWORD=your-password-here
